# Secretos y entornos
.env
.env.local
.env.*.local
.vercel/

# Dependencias
node_modules/

# Archivos temporales / scratch
*.backup-*
*.bak
*.tmp
*.tmp-*

# Diagnósticos y auditorías de sesión (no van a producción)
AUDIT_*.txt
DIAGNOSTICO_*.txt
VERIFICAR_*.txt
*_PARA_AUDITORIA.txt
*_PARA_AUDIT.txt

# Sistema operativo
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# ═══════════════════════════════════════════════════════════════
# CONSOLIDACIÓN MAYO 2026 — limpiar ruido del filesystem local
# ═══════════════════════════════════════════════════════════════

# Auditorías de sesión / handoff (no commitear)
*audit*.txt
DIAG_*.txt
handoff_*.txt
index_audit.txt

# Nested repo de triggui-content (workflows lo crean fresh en CI con actions/checkout)
triggui-content/

# Caches operacionales del motor (se regeneran en cada run)
experiments/nucleus/evidence-cache/
experiments/nucleus/grounding-cache/

# Snapshots y reports históricos (output de runs, no source)
experiments/nucleus/inputs-history/
experiments/nucleus/quality-reports/

# Outputs sample/test del motor (el contenido.json canónico vive en triggui-content)
experiments/nucleus/contenido.json
experiments/nucleus/contenido.shadow.json
experiments/nucleus/contenido_edicion.json
experiments/nucleus/contenido_manual.json
experiments/nucleus/contenido.legacy.test.json
experiments/nucleus/contenido.external.test.json
experiments/nucleus/contenido.external.test.v2.json
experiments/nucleus/lab-pulse.json
experiments/nucleus/metrics/

# Mobile build artifacts
mobile/.expo/

# Node deps
node_modules/

# Backups locales
*.backup-*
*.tmp-*

# ─── Outputs del motor v3.7.1+step3 cuando se corre desde raíz del repo ───
# (los del subfolder experiments/nucleus/ ya estaban arriba)
/evidence-cache/
/grounding-cache/
/inputs-history/
/quality-reports/
/metrics/

# Apps Script local config (clasp)
apps-script/.clasp.json

# Workflow backups (no commitear)
.github/workflows/*.yml.bak-pre-*
.github/workflows/*.yml.backup-*
.github/workflows/*.yml.pre-*

# ════════════════════════════════════════════════════════════════
# 🛡️ Anti-contaminación local (post audience-guard sprint 2026-05-18)
# ════════════════════════════════════════════════════════════════

# Backups locales (cualquier patrón)
*.bak
*.bak-*
*.bak-pre-*
*.bak-audit-*
*.bak-sanitize-*
*.bak-svg-*
*.pre-*.bak
*.backup-*
*.backup
_backup-pre-*/
**/_backup-pre-*/

# Scripts de migración históricos (deben quedarse locales)
aplicar-*.py
aplicar-*.sh
bump-version-*.py
inyectar-*.py
auditoria-nivel-dios.sh
yml-actual-completo.yml
apps-script-backups/

# Python compilados
__pycache__/
*.pyc
*.pyo

# IDE / OS
.DS_Store
.vscode/
.idea/

# Debug del nucleus (no productivo)
validate-debug/
